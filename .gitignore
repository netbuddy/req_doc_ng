# OS and editor files
.DS_Store
Thumbs.db
*~
.swp
.swo
.idea/
.vscode/

# Local environment and secrets
.env
.env.*
!.env.example
*.local
*.secret

# Python cache and virtual environments
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

# Node and frontend build output
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
dist/
build/
coverage/
*.tsbuildinfo

# Logs and temporary files
*.log
logs/
tmp/
temp/
.cache/

# Agent 会话本地目录（不入库）
.claude/

# backend 运行时产物目录（导出的 docx/PDF 等，跑出来的东西；不入库）。
# 注意：图形渲染构件 plantuml.jar / puppeteer.json 在 backend/tools/ 并入库——
# 它们是构建镜像的必需输入，不入库会让换机器构建出的镜像静默缺图形功能。
backend/var/
